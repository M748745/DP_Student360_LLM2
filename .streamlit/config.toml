
[server]
# For Streamlit Cloud: DO NOT set address (defaults to 0.0.0.0)
# For local development: uncomment the line below
# address = "localhost"
port = 8501

# Optional: Disable auto-opening browser
headless = false

# Optional: Enable CORS for development
enableCORS = false

# Optional: Enable XSRF protection
enableXsrfProtection = true
